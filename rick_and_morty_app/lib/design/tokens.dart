import 'package:flutter/material.dart';

class Tokens {
  static const bgDark = Color(0xFF2F3336); // fundo da tela
  static const cardBlue = Color(0xFF8FD1E8); // cards e blocos
  static const textPrimary = Colors.white; // texto em fundo escuro
  static const textSecondary = Color(0xFF24323A); // texto em card azul
  static const radius = 12.0;
  static const gap = 12.0;
}
